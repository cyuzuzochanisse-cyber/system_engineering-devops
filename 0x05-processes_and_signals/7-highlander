#!/usr/bin/env bash

# Trap SIGTERM and exit properly after printing the message
trap 'echo "I am invincible!!!"; exit 0' SIGTERM

while true; do
  echo "To infinity and beyond"
  
  # Run sleep in the background and wait for it
  sleep 2 &
  wait $!
done
